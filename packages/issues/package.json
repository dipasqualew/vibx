{
  "name": "@vibx/issues",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "@octokit/graphql": "^8.2.1"
  },
  "devDependencies": {
    "dotenv": "^16.5.0"
  }
}
