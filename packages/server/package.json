{
  "name": "@vibx/server",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "test": "vitest run --passWithNoTests",
    "build": "bun build src/index.ts --outdir dist --target=node"
  },
  "dependencies": {
    "@vibx/issues": "workspace:*",
    "@vibx/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8"
  }
}
